# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
orjson==3.10.6                       # Fast JSON serialiser

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0                      # Async PostgreSQL driver
alembic==1.13.1
psycopg2-binary==2.9.9               # Sync driver for Alembic offline mode

# ─── Redis ────────────────────────────────────────────────────────────────────
redis[hiredis]==5.0.4

# ─── Celery ───────────────────────────────────────────────────────────────────
celery[redis]==5.4.0
kombu==5.3.4

# ─── Auth ─────────────────────────────────────────────────────────────────────
PyJWT[crypto]==2.8.0                 # RS256 JWT
argon2-cffi==23.1.0                  # Argon2id password hashing
python-jose[cryptography]==3.3.0     # Fallback / OAuth token verification

# ─── Config / Validation ──────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
pydantic[email]==2.7.1
python-multipart==0.0.9              # Form data parsing

# ─── Storage ──────────────────────────────────────────────────────────────────
boto3==1.34.120

# ─── HTTP client (inter-service calls from Celery tasks) ─────────────────────
httpx==0.27.0

# ─── Observability ────────────────────────────────────────────────────────────
structlog==24.2.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
prometheus-client==0.20.0

# ─── Dev / Test ───────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0                        # Also test client
factory-boy==3.3.0

# Stage 3: Auth & User Management additions
httpx==0.27.0            # Google OAuth token exchange
pyotp==2.9.0             # TOTP MFA (Google Authenticator)
qrcode[pil]==7.4.2       # QR code generation for MFA setup
orjson==3.10.6           # Fast JSON serialiser (ORJSONResponse)
python-multipart==0.0.9  # Form data (already included, ensure present)
user-agents==2.2.0       # Parse User-Agent strings for session device type

# Stage 8: Style DNA System
reportlab==4.2.0        # PDF generation
Pillow==10.4.0          # Image manipulation for PDF thumbnails
